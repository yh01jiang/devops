apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prometheus-ingress
  namespace: tensuns
spec:
  tls:
    - hosts:
        - "barry-qaprom.*.com"
      secretName: tls-*.com
  rules:
    - host: "barry-qaprom.*.com"
      http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: prometheus
                port:
                  number: 9090
